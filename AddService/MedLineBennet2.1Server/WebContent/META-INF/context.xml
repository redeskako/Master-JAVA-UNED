<?xml version="1.0" encoding="UTF-8"?>
 <!-- El contenido de este contexto de aplicación se cargará automáticamente en 
 el archivo server.xml al desplegar la aplicación, por lo que estará disponible
 para todas las aplicaciones que se ejecuten en el servidor.
 
 Al recibir una petición http, Tomcat(Catalina) le agregará el contexto correspondiente
 en función de su URL. Pueden existir diferentes contextos para una misma aplicación,
 se cargará uno u otro en función de la URL.
 
 Separando la configuración en este archivo se separan los roles del desarrollador y
 de la persona que despliega la aplicación. El desarrollador puede crear la aplicación
 sin detenerse en la configuración de los recursos que serían configurados por 
 quien despliegue la aplicación web.  -->
 
 <!-- crossContext=true: Una aplicación web puede acceder a otra aplicación web desplegada en el mismo servidor
 Solo se suele deshabilitar para aplicaciones que requieran mucha seguridad. -->
  <Context crossContext="true">
 
    <!-- WatchedResource - The auto deployer will monitor the specified static resource of the web application
     for updates, and will reload the web application if it is updated.
      The content of this element must be a string. -->
    <WatchedResource>WEB-INF/web.xml</WatchedResource>
 
     <!-- Diferentes configuraciones para la conexión que se podrían incluir en el recurso -->
        <!-- maxActive="100" maxIdle="30" maxWait="10000"  -->
   
   <Resource name="jdbc/medlineDB" auth="Container"
        type="javax.sql.DataSource"
        username="root"
        password="root"
        driverClassName="com.mysql.jdbc.Driver"
        url="jdbc:mysql://localhost/medlinebennett"/>
</Context>